apiVersion: v1
kind: ConfigMap
metadata:
  name: pgadmin-config
data:
  servers.json: |
    {
        "Servers": {
          "1": {
            "Name": "Kyberlife",
            "Group": "Kyberlife",
            "Port": 5432,
            "Username": "guest",
            "Host": "svc-postgresql",
            "SSLMode": "prefer",
            "MaintenanceDB": "postgres"
          }
        }
    }

apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: admin-role-binding
  namespace: default
roleRef:
  apiGroup: ""
  kind: Role
  name: admin
subjects:
  - kind: ServiceAccount
    namespace: default
    name: default

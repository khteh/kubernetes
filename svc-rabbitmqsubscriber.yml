apiVersion: v1
kind: Service 
metadata:
  name: svc-rabbitmq-subscriber
#  annotations:
#    alb.ingress.kubernetes.io/healthcheck-path: /rabbitmq-subscriber/actuator/health
  labels: 
    component: rabbitmq-subscriber
    app: rabbitmq-subscriber
spec:
  ports:  
  - name: http
    protocol: TCP
    port: 5000
    targetPort: 5000
  - name: https
    protocol: TCP
    port: 443
    targetPort: 443
  clusterIP: None # Headless. type cannot be LoadBalancer
  selector:
    app: rabbitmq-subscriber
#    tier: frontend
#  type: LoadBalancer # Exposes web to traffic from outside of the cluster 
#---
#apiVersion: v1
#kind: Service 
#metadata:
#  name: svc-rabbitmq-subscriber-ingress
#  labels: 
#    app: rabbitmq-subscriber
#spec:
#  ports:  
#  - name: http
#    protocol: TCP
#    port: 5000
#    targetPort: 5000
#  - name: https
#    protocol: TCP
#    port: 443
#    targetPort: 443
#  type: NodePort
#  selector:
#    app: rabbitmq-subscriber
